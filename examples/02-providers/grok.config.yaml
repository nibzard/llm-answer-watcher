# Grok Provider Example
# Demonstrates X.AI Grok models
# Cost: ~$0.015 per run (3 intents × grok-beta)

run_settings:
  output_dir: "./output"
  sqlite_db_path: "./output/watcher.db"

  models:
    - provider: "grok"
      model_name: "grok-beta"
      env_api_key: "GROK_API_KEY"
      # Uses default grok system prompt

brands:
  mine: ["YourBrand", "YourProduct"]
  competitors: ["CompetitorA", "CompetitorB", "CompetitorC"]

intents:
  - id: "best-tools"
    prompt: "What are the best project management tools?"

  - id: "tool-comparison"
    prompt: "Compare the top project management platforms for teams"

  - id: "buying-guide"
    prompt: "How do I choose the right project management software?"
